[package]
name = "meow_os"
version = "0.1.0"
authors = ["Jules"]
edition = "2021"



[lib]
name = "meow_os"
test = false
bench = false

[dependencies]
bootloader = { version = "0.9.23", features = ["map_physical_memory"] }
volatile = "0.2.7"
lazy_static = { version = "1.0", features = ["spin_no_std"] }
spin = "0.5.2"
x86_64 = "0.14"
uart_16550 = "0.2"
pic8259 = "0.10"
pc-keyboard = "0.7"
heapless = "0.7"
linked_list_allocator = "0.10.5"

# Define a feature called allocator_debug
[features]
allocator_debug = []

# The profile used for `cargo build`
[profile.dev]

# The profile used for `cargo build --release`
[profile.release]

[package.metadata.bootimage]
test-args = ["-device", "isa-debug-exit,iobase=0xf4,iosize=0x04", "-serial", "stdio", "-display", "none"]
test-success-exit-code = 33
test-timeout = 10


[[bin]]
name = "meow_os"
test = false
bench = false

